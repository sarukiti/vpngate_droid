<resources>
    <!-- Base application theme. -->
    <style name="Theme.Android_vpngate" parent="Theme.Material3.Dark.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>
</resources>